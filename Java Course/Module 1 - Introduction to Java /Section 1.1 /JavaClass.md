# Section 1.1 - Java Class

Classes are the fundamental building blocks of Java programming. You specify every component and feature of one of those building elements when you define a class. Most classes need you to build objects in order to use them. A run instance of a class in memory is called an object. The state of your program is represented by all the different objects of all the different classes.
